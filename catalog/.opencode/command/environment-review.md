---
description: Review a project's OpenCode environment and produce an approved plan
agent: explore
---

Use `opencode-env review --project $ARGUMENTS` from this repository. Inspect the target project without reading secrets, then recommend only the minimal agents, skills, commands, rules, MCP servers, and verification commands it needs. Save the recommendation as an explicit plan and do not apply it automatically.
